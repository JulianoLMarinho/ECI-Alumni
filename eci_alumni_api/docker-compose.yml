version: '3.5'
volumes:
  data:
services:
  db:
    container_name: eci_alumni_db
    image: mysql:5.7
    ports:
      - "3306:3306"
    volumes:
      - data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=alumni
